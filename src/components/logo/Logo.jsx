import styled from "styled-components"

const LogoContainer = styled.div``
const LogoText = styled.h4``

const Logo = () => {
    return (
        <LogoContainer>
            <LogoText>D4WKINS</LogoText>
        </LogoContainer>
    )
}

export default Logo
